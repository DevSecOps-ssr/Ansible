- name : demo on filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, ansible, terraform
  tasks:
  - name : greeting
    ansible.builtin.debug:
      msg: "Hi Sivaram, {{GEETING | default('Good morning')}}"

  - name : convert string to list
    ansible.builtin.debug:
      msg: "{{topics | split(',')}}"